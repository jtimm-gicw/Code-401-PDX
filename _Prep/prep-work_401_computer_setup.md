# Code 401 Computer Setup

## Follow thses instructions to install packages needed for this section.

### Copy & Paste these commands into your terminal...

#### Install commands

1. `sudo apt install -y httpie`

2. `sudo apt install -y postgresql postgresql-contrib`

**Start Postgresql**
Start the service for postgresql after installation of the packages
3. `sudo systemctl start postgresql` or `sudo service postgresql start`